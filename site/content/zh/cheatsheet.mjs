/* 速查表自己的内容。

   SPINE 是贯穿整门课、最后还留得下来的那几条主线；它们是读者应该能背出来的
   部分。KEYS 是每一章浓缩出来的一对：`claim` 是这一章立住的判断，`counter`
   是你能拿它怎么办、以及到哪里就不管用了。/cheatsheet/ 上其余的一切都由已经
   存在的集合拼出来——威胁地图、防御地图和课程大纲——所以它不会和它们走散。 */

export const SPINE = [
  [`上下文是一条扁平序列`,
   `没有特权通道，没有带外的控制平面，transformer 也没有任何相当于参数化查询的东西。模型分不出哪条指令是别人交给它的，哪条是它自己读来的。凡是依赖模型把两者区分开的防御，都只能抬高攻击者的成本；没有一个能给最坏情况封顶。`],
  [`循环本身就是那个漏洞`,
   `智能体把陌生人挑好的字节，追加进那个装着目标的同一个上下文，然后拿着主体自己的权限去调用工具。找到这些字节进来的那条线，你就找到了信任边界。这门课其余的一切，都是关于那一根箭头的问题。`],
  [`先问那四个问题`,
   `有什么会进入上下文，而且陌生人能写它？智能体能做什么，用谁的权限做？什么能出去，走哪条路？什么是不可逆的？对一个真实系统把这四问答完，威胁模型自己就写出来了。`],
  [`限制损害，而不是检测攻击`,
   `一个在模型彻底沦陷时仍然成立的控制，胜过一个一百次里抓住九十九次的分类器。检测买来的是攻击者的时间，架构买来的是一个上界。读到一条缓解措施，先问它属于哪一种。`],
  [`把监督花在撤不回来的事情上`,
   `人工审批是一份固定预算，不是免费的控制。按撤销难度给动作排序，把预算花在这份清单的最上面。每一次多余的弹窗，都是在训练操作员把真正要紧的那次也顺手点掉。`],
  [`要么做自适应评估，要么就别评估`,
   `静态基准测试量的是有人在你上线之前想到过的攻击。把防御摊开给攻击者看，让他再试一次，报出那个数字。一个只在自己那套测试集上被测过的防御，等于没被测过。`],
];

export const KEYS = {
  a01: {
    claim: `每个智能体都是同一个循环：把工具返回追加进与用户目标同一个上下文，然后拿着主体自己的权限去调用工具。`,
    counter: `用四个问题审问任何一个智能体：注入面、行动面、外泄通道、不可逆动作。叫模型别听检索来的指令，什么上界都给不了。`,
  },
  a02: {
    claim: `角色被压平成一条 token 序列，于是系统、用户和工具文本在同等条件下竞争。transformer 没有参数化查询这回事。`,
    counter: `每次请求现生成的随机哨兵伪造不了，固定的围栏则不然。但载荷只要待在被标记的区域里客气地开口请求，就没有任何东西拦得住。`,
  },
  a03: {
    claim: `一个智能体只有同时握着私有数据、会接触不可信内容、又存在字节向外流出的路径时，才可能被用来窃取数据。`,
    counter: `切断出站通常最划算：一份出站允许清单能兜住一次彻底的劫持。三元组是必要条件，不是充分条件；搞破坏和给出坏答案都不需要对外通道。`,
  },
  a04: {
    claim: `SEI 的分类法梳理出六个威胁面和二十五类漏洞，于是一次评审覆盖的是整片区域，而不是你碰巧想得起来的那些风险。`,
    counter: `当清单来跑，每一条“不适用”都要落到纸面上讲清楚。没有哪一份资料握着完整的地图；作者的背景基本能预测他漏掉了什么。`,
  },
  a05: {
    claim: `把 STRIDE、MAESTRO、OWASP、ATLAS 和杀伤链用在同一个智能体上，得到的发现各不相同，因为每套框架的结构决定了它看不见什么。`,
    counter: `有意识地挑一片镜片，并说清它漏掉什么，而不是把五套摞在一起。覆盖度来自把盲区点名，不是来自多跑几套框架。`,
  },
  a06: {
    claim: `直接注入里攻击者就是用户，而那个实验演示的是关键词黑名单输给一个无穷的目标集合，以及一个迭代更快的对手。`,
    counter: `过滤器留着做遥测、挡便宜流量，并且把系统 prompt 当公开信息看待。过滤抬高攻击者成本；它从不限制一次成功能拿到什么。`,
  },
  a07: {
    claim: `间接注入把载荷埋进智能体会检索到的内容里，于是一个被投毒的页面就劫持了每一个触及它的任务，攻击者连一次会话都不需要。`,
    counter: `用代码切断最后一环：固定的能力集合，以及只抓取用户点名的 URL。模型照样被劫持，只是这次劫持什么也拿不到。`,
  },
  a08: {
    claim: `计算机操作类智能体依据截图、DOM 转储和无障碍树行动，所以指令可以藏在监督者在屏幕上确实永远看不到的文本里。`,
    counter: `坐标不带任何语义，策略只能从无障碍树重建出来。对这类智能体，一台没有任何已登录会话的一次性虚拟机才是首要控制。`,
  },
  a09: {
    claim: `数据有八条通道可走——渲染出来的图片、链接预览、DNS、文件写入——其中大多数发出请求的是客户端，不是智能体。`,
    counter: `一份扛得住后缀、userinfo 和 DNS 重绑定花招的出站允许清单，加上渲染侧的客户端 CSP。两者都拦不住用户去点，也拦不住一个合法的收件人。`,
  },
  a10: {
    claim: `被劫持的智能体不偷任何凭据；它重定向的是一个本来就攥着凭据的代理，于是恶意请求认证完全正确，审计日志看上去干干净净。`,
    counter: `收窄授权，按任务削权，把溯源带进运行时。给不可逆动作加人工关卡只是抬高成本，而且会在疲劳下退化。`,
  },
  a11: {
    claim: `工具描述就是 prompt 文本，所以装一个 MCP 服务器等于在改你的指令——早在工具被调用之前，此后服务器每更新一次还会再改一次。`,
    counter: `按内容哈希钉住清单，一有变动就重新征求同意；每个服务器各跑在自己的沙箱里，配自己范围受限的凭据。描述绝不能扩大它自己的权限范围。`,
  },
  a12: {
    claim: `给语料库或记忆库投毒，买到的是持久性：一次编辑会在此后每一次检索、对每一个用户触发，直到有人发现并把它删掉。`,
    counter: `每一次记忆写入都记溯源，绝不从工具返回自动写入，按用户隔离并设 TTL。删掉那个恶意来源，并不能撤销智能体已经存下的东西。`,
  },
  a13: {
    claim: `技能是智能体会照着执行的指令文本，这让它成为一个比同等体量和流行度的库更危险的供应链依赖。`,
    counter: `按哈希钉住，安装走允许清单，绝不在使用时按名字解析一个技能。扫描抓不住组合：两个各自安全的技能能串成一条外泄流水线。`,
  },
  a14: {
    claim: `后门活在权重里，它的触发条件也不必长得像一条指令，所以每一种检查 prompt 的防御在结构上都看不见它。`,
    counter: `供应链卫生——校验权重摘要、管住微调数据——再加上围堵，让触发之后够得着的东西不值得拿。检查这个选项根本不存在。`,
  },
  a15: {
    claim: `一条命令宿主转发自己的注入会在智能体之间扩散，而三元组会沿着可达路径组合起来，尽管路径上每一个单独节点都干干净净。`,
    counter: `给路径打分，不是给节点打分，并且每一次交接都带上溯源和明说的不确定度。评审智能体能减少错误，同时也扩大了一次沦陷所能触及的面。`,
  },
  a16: {
    claim: `什么都没偷走的攻击照样烧钱。扇出、递归循环和慢慢变大的工具返回，能在不涉及任何漏洞的情况下把开销抬高到 658 倍。`,
    counter: `按运行、按身份、按工具、按租户分别设预算。只有按身份的上限能堵住钱包耗尽；给工具返回大小设上限则直接干掉那个放大器。`,
  },
  a17: {
    claim: `在真实的基率下，一个真正例率 99%、假正例率 1% 的检测器精确率不到 1%，于是分析师学会无视每一条告警——而且这么做是对的。`,
    counter: `把分类器放在工具返回这条路径上，也就是不可信字节抵达的地方，并且只把它当成加在攻击者身上的成本，绝不当成给最坏情况封顶的东西。`,
  },
  a18: {
    claim: `每次请求现生成的随机哨兵让不可信区域无法伪造，但决定听不听的仍然是模型。标记给你的是一条边界，不是一条打不破的规则。`,
    counter: `给每一段不可信内容做数据标记，并优先选指令层级或 SecAlign 训练；两者都只是抬高成本。只有 Jatmo 真正拿掉了指令跟随，代价是每个任务一个微调模型。`,
  },
  a19: {
    claim: `一个在固定基准上攻击成功率 0% 的防御，面对读过它源码之后写出来的载荷可以到 100%。基准测试量的是已知攻击，不是安全性。`,
    counter: `带着明说的攻击者预算，针对你自己的防御跑自适应测试，并在同一次运行里报出效用留存；把智能体拔了，0% 唾手可得。`,
  },
  a20: {
    claim: `六种模式靠拿走一项能力来给注入设上界：动作选择器、先规划后执行、双 LLM、先生成代码再执行、map-reduce、上下文最小化。每一条保证都是用能力换来的。`,
    counter: `按任务的形态来选，并且为你交出去的那部分能力做辩护。没有一种能让输出变得可信，而在不可信内容上做开放式行动，压根没有安全的架构。`,
  },
  a21: {
    claim: `给每个值打上来源和读者的标签，来源取并集，读者取交集；汇点上的策略自始至终都不去读那段不可信文本。`,
    counter: `改写、编码和混合都洗不掉一个标签。代价在于写这条策略、维护一个解释器，以及过度污染——后者需要显式且受审计的降级点。`,
  },
  a22: {
    claim: `削权就是取交集，所以子 token 只能比父 token 更窄。一条注入进来的、索要 <code>repo.admin</code> 的指令，换来的是更少的权限而不是更多。`,
    counter: `按运行签发凭据，在范围、资源和有效期上一并收窄，并把委派链一路带着，让审计说得出是哪个智能体动的手，而不只是哪个用户。`,
  },
  a23: {
    claim: `影响范围由环境和它的执行点决定，不由智能体的意图决定。一旦智能体开始运行生成出来的代码，进程内的出站检查就只是一份文档。`,
    counter: `解析 URL 并比对主机名，检查之前先归一化路径，在 pod 或代理上强制执行。围堵限的是触及范围；它从不修复一个被攻击者影响过的答案。`,
  },
  a24: {
    claim: `审批注意力是一份共享而且会耗尽的预算，所以给一个可逆动作加确认，花的是守着不可逆动作的那同一个账户。`,
    counter: `按可逆性给工具分级，只给撤不回来的那些设关卡。把将要发出去的数据、收件人、指令的溯源，以及一个中止任务的选项摆到人面前。`,
  },
  a25: {
    claim: `每一个公开基准都共用一套固定的攻击集合，所以一个好分数说的是已知攻击，而且没有一个覆盖你的工具、数据或策略。`,
    counter: `搭一套评估框架，让攻击成功率和效用出自同一次运行，公布用例清单，并且把这次评估没有覆盖的东西写下来。`,
  },
  a26: {
    claim: `四个字段让日志可被调查：<code>caused_by</code>、<code>provenance</code>、链式哈希和策略拒绝。没有因果，轨迹只说得出发生了什么，永远说不出为什么。`,
    counter: `把因果祖先不可信的动作标出来，慢速通道的信号跨会话算而不是按请求算，元数据留得久，内容则留得短、留得脱敏。`,
  },
  a27: {
    claim: `36 个真实案例研究里，超过 60% 描述到的控制类别不足 33 类中的五类，这让<b>行业标准</b>成了一条低门槛，而不是一个目标。`,
    counter: `每个部署阶段都用通过标准和一条你今天就能跑的回滚路径来把关。不用发一次版就能全局停机才叫架构，而溯源必须早于事件存在。`,
  },
};
