/* 读者在课程中会遇到的术语，在此统一定义。`ch` 指向它被讲授的地方。 */
export const TERMS = [
  ['智能体', 'core', `一个系统，其感知、规划和行动选择由 LLM 驱动，并且不需要人类居中处理每一步
    就能对世界采取行动。通常被引用的四条性质是：追求未被完全指定的目标、直接行动、目标导向的
    行为，以及长周期规划。`, 'a01'],
  ['智能体循环', 'core', `每个智能体正中央的那个 while 循环：从上下文拼出 prompt，调用模型，
    解析出一个动作，执行它，把结果追加回去，如此重复直到完成。`, 'a01'],
  ['信任边界', 'core', `数据信任级别发生变化的那条线。在智能体里，关键的那条位于工具返回处：
    由主体之外的人撰写的内容，进入了与主体指令同一条 token 流。`, 'a02'],
  ['指令与数据混淆', 'core', `提示注入的根因。transformer 消费的是一条扁平序列，其中没有任何
    通道能在结构上把命令与内容区分开。`, 'a02'],
  ['致命三元组', 'core', `Simon Willison 用来概括那个让智能体可被利用的组合的说法：能访问私有
    数据、会接触不可信内容、具备对外通信的能力。去掉其中任意一条，数据外泄的路径就断了。`, 'a03'],
  ['威胁面', 'core', `按漏洞从哪里进入系统来给它们分组。SoK 的分类法用了六个：直接、间接、内部、
    资源、监督和复合。`, 'a04'],
  ['MAESTRO', 'framework', `一套专为智能体系统构建的七层威胁建模框架，自下而上从基础模型一直
    延伸到智能体生态。`, 'a05'],
  ['MITRE ATLAS', 'framework', `Adversarial Threat Landscape for AI Systems：一张 ATT&CK 风格的
    矩阵，收录已在 ML 与 AI 系统上观察到的战术与技术。`, 'a05'],
  ['OWASP ASI', 'framework', `OWASP Agentic Security Initiative，在 OWASP Top 10 for LLM
    Applications 之外，另行发布智能体的威胁与缓解指南。`, 'a05'],

  ['直接提示注入', 'attack', `主体用户提供的输入，被设计用来覆盖开发者的指令。与越狱有重叠，
    但并不等同。`, 'a06'],
  ['越狱', 'attack', `诱使模型违反自身安全策略的输入。那是内容安全的失效；提示注入则是权限的
    失效。`, 'a06'],
  ['间接提示注入', 'attack', `把指令种进智能体将会检索到的内容里——一个网页、一封邮件、一份
    文档、一条代码注释——随后智能体把它当作意图执行。`, 'a07'],
  ['环境注入', 'attack', `经由计算机操作类智能体所感知的那块渲染界面送达的间接注入：弹窗、图片、
    DOM 节点、无障碍标签。`, 'a08'],
  ['数据外泄通道', 'attack', `任何由智能体可控、能把数据送到攻击者手里的路径：一个被渲染的
    markdown 图片 URL、一次向外的工具调用、一次 DNS 查询、一条用户会点的链接。`, 'a09'],
  ['混淆代理', 'attack', `一个有特权的组件被诱导，替一个权限更低的一方滥用自己的权限。手里攥着
    用户 OAuth token 的智能体，就是教科书式的代理。`, 'a10'],
  ['过度自主权', 'attack', `OWASP LLM06：智能体持有的权限、自主性或功能超出任务所需，于是一次
    沦陷造成的损害更大。`, 'a10'],
  ['工具投毒', 'attack', `放进工具元数据里的恶意指令，比如藏在名称、描述或参数文档中，早在工具
    被调用之前就已经抵达模型。`, 'a11'],
  ['插队（line jumping）', 'attack', `被投毒的 MCP 服务器在工具列举阶段就影响智能体，此时用户
    还没有批准或调用它的任何东西。`, 'a11'],
  ['抽梯子（rug pull）', 'attack', `一个工具或技能在被批准时是良性的，此后才发生变异；多数客户端
    会重新读取元数据，却不会重新征求批准。`, 'a11'],
  ['工具遮蔽', 'attack', `一个恶意服务器用特定方式描述自己的工具，从而改变智能体使用另一个受信任
    服务器的工具的方式。`, 'a11'],
  ['记忆投毒', 'attack', `把攻击者可控的内容写进智能体的持久记忆或知识库，让攻击在未来毫不相干的
    会话里重放。`, 'a12'],
  ['PoisonedRAG', 'attack', `针对检索增强生成的知识污染攻击。精心构造少量文档，让它们既能在目标
    查询上排到前面，又能决定最终答案。`, 'a12'],
  ['技能', 'core', `一份打包好的指令（往往还带着代码），智能体加载它来获得某项能力。像软件包一样
    分发的可执行散文，通常却没有软件包那套工具链。`, 'a13'],
  ['模型后门', 'attack', `被训练进权重里的一种行为，遇到触发条件才激活，在 prompt 层面的检查中
    完全看不见。`, 'a14'],
  ['提示感染', 'attack', `一种多智能体注入，其载荷指示每一个受害者继续转发该载荷，从而在拓扑中
    形成蠕虫式传播。`, 'a15'],
  ['钱包耗尽', 'attack', `以金钱计量的资源耗尽：逼迫智能体进入漫长的推理链或工具调用风暴，账单
    落在运营方头上。`, 'a16'],

  ['护栏', 'defense', `作用在智能体输入、输出或动作上的过滤器。可以是正则、分类器、策略引擎，
    也可以是另一个模型。它是概率性的，就其构造而言必然可被绕过。`, 'a17'],
  ['聚光标记', 'defense', `把不可信内容标记出来，好让模型能把它区分开：分隔符、数据打标（在整段
    内容中穿插一个标记）或编码。这是 Microsoft 对防御性 prompt 的提法。`, 'a18'],
  ['指令层级', 'defense', `训练模型按特权高低给指令来源排序（系统高于开发者，开发者高于用户，
    用户高于工具输出），并拒绝来自低特权来源的覆盖。`, 'a18'],
  ['StruQ / SecAlign', 'defense', `训练期的防御，教模型把结构化的数据通道当成数据：StruQ 走结构化
    指令微调，SecAlign 走偏好优化。`, 'a18'],
  ['自适应攻击', 'defense', `针对正在被评估的那个具体防御调校过的攻击。这是衡量一项防御唯一诚实的
    方式；静态基准测试会系统性地高估防护效果。`, 'a19'],
  ['攻击成功率（ASR）', 'defense', `达成攻击者目标的尝试所占的比例。只有与效用留存和攻击者的预算
    一同报告时才有意义。`, 'a19'],
  ['效用留存', 'defense', `一项防御上线之后，良性任务的性能还剩下多少。一个 ASR 为 0%、效用只有
    40% 的防御并没有解决问题。`, 'a19'],
  ['动作选择器模式', 'defense', `智能体只能在一组预先批准的固定动作里做选择，而且永远看不到工具
    返回。没有反馈回路，也就没有注入路径。`, 'a20'],
  ['先规划后执行', 'defense', `计划的每一步都在任何不可信内容被检索之前就已固定，因此被注入的
    文本能影响参数，却影响不了控制流。`, 'a20'],
  ['双 LLM', 'defense', `一个从不接触不可信内容的特权模型，去编排一个接触不可信内容的隔离模型，
    两者之间只交换不透明的变量引用。`, 'a20'],
  ['CaMeL', 'defense', `Capabilities for Machine Learning：先从可信的用户查询中把控制流抽取成一段
    Python 子集程序，再对隔离模型产出的每一个值执行能力策略。`, 'a21'],
  ['信息流控制（IFC）', 'defense', `给每一个值打上溯源与完整性级别的标签，然后拒绝那些会把被污染
    的值送往它不该抵达的汇点的操作。`, 'a21'],
  ['污点追踪', 'defense', `IFC 的实用形态：标记来自不可信来源的数据，并让这个标记沿着每一个派生
    出来的值继续传播。`, 'a21'],
  ['削权 token', 'defense', `在交给智能体之前，按范围、受众、资源和有效期刻意收窄过的凭据，这样
    即使 token 被偷走，它也值不了多少。`, 'a22'],
  ['最小权限', 'defense', `Saltzer 与 Schroeder 的原则：每个组件都只以完成工作所需的最小权限运行。
    放到智能体上，就是按任务而不是按会话来划分权限集。`, 'a22'],
  ['出站允许清单', 'defense', `只放行指定目的地的网络策略。它是对抗数据外泄最有效的单项控制，
    因为它打断了三元组的第三条腿。`, 'a23'],
  ['沙箱化', 'defense', `在一个隔离环境里执行由智能体驱动的代码或工具，只给最低限度的文件系统、
    网络和进程权限。`, 'a23'],
  ['人工确认（HITL）', 'defense', `在一个动作执行之前必须由人做出的决定。它的价值会在审批疲劳下
    崩塌，所以必须为它编预算。`, 'a24'],
  ['可逆性等级', 'defense', `按撤销的难度给动作分级，从可逆、代价高昂一直到不可逆，然后把你的
    打断预算花在最后那一类上。`, 'a24'],
  ['AgentDojo', 'eval', `一套动态基准测试，包含贴近现实的任务和注入攻击，为调用工具的智能体同时
    衡量效用与攻击成功率。`, 'a25'],
  ['Agent Security Bench (ASB)', 'eval', `一套形式化的基准测试，覆盖十个场景、大量工具，以及一个
    针对 LLM 智能体的攻击与防御矩阵。`, 'a25'],
  ['InjecAgent', 'eval', `针对工具集成型智能体中间接提示注入的基准测试，把直接造成危害与窃取数据
    这两类攻击者目标区分开。`, 'a25'],
  ['轨迹', 'ops', `一次智能体运行的完整有序记录：prompt、模型输出、工具调用、工具返回，以及它们
    之间的各个决策。它是智能体取证的基本单位。`, 'a26'],
  ['任务漂移', 'ops', `用户所要求的与智能体当下正在追求的之间的偏离。可以从激活值的差异或轨迹级
    分析中检出。`, 'a26'],
  ['纵深防御', 'ops', `把异构的控制一层层叠起来，让某一层漏掉的攻击被另一层抓住。这是整个文献里
    的共识建议。`, 'a27'],
  ['分阶段部署', 'ops', `在安全指标达标之后分阶段放宽智能体的作用范围，每个阶段都留一条回滚
    路径。`, 'a27'],
];
